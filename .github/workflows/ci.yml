name: CI

on:
  push:
    branches:
      - main
    paths:
      - 'Makefile'
      - 'htdocs/**'
      - 'root/**'
      - 'po/**'
      - '.github/workflows/**'

permissions:
  contents: read

concurrency:
  group: ci-main
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Validate source
        shell: bash
        run: |
          set -euo pipefail

          find htdocs -type f -name '*.js' -print0 | xargs -0 -r -n1 node --check

          python3 - <<'PY'
          import json
          from pathlib import Path

          for path in Path('root').rglob('*.json'):
              json.loads(path.read_text(encoding='utf-8'))
          PY

          sh -n root/etc/init.d/frpc-advanced
          sh -n root/etc/uci-defaults/40_luci-frpc-advanced

          PKG_VERSION="$(sed -n 's/^PKG_VERSION:=//p' Makefile)"
          PKG_RELEASE="$(sed -n 's/^PKG_RELEASE:=//p' Makefile)"

          [[ "$PKG_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]
          [[ "$PKG_RELEASE" =~ ^[0-9]+$ ]]

          test -s po/templates/frpc-advanced.pot
          test -s po/zh_Hans/frpc-advanced.po
